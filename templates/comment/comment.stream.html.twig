<turbo-stream action="append" targets="#lesson_comments_{{ lesson.id }}">
    <template>
        {{ include('comment/_comment.html.twig',{
            comment: comment
        }) }}
    </template>
</turbo-stream>