<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/x-icon" href="{{ asset('images/white-logo.ico') }}"/>
    <meta name="keywords" content="english teacher prague">
    <meta property="og:title" content="Learn English via video call or in-person lessons in Prague"/>
    <meta property="og:description" content="Join our online English video call or in-person lessons in Prague with native speakers. Improve your skills through conversational practice, interactive quizzes, and tailored learning for all levels!"/>
    <title>{% block title %}Talk English{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100..900;1,100..900&family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap"
              rel="stylesheet">
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}

        {% if app.environment == 'prod' %}
            <!-- Google tag (gtag.js) -->
            <script async src="https://www.googletagmanager.com/gtag/js?id=G-7FPQ0R1J5F"></script>
            <script>
                window.dataLayer = window.dataLayer || [];

                function gtag() {
                    dataLayer.push(arguments);
                }

                gtag('js', new Date());

                gtag('config', 'G-7FPQ0R1J5F');
            </script>
        {% endif %}
    {% endblock %}
</head>
<body data-bs-theme="light">
{{ include('partial/_navbar.html.twig') }}

<div class="container">
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }} border-0 my-3 alert-dismissible fade show">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}
    {% endfor %}
</div>

<div class="min-vh-70">
    {% block body %}{% endblock %}
</div>
{{ include('partial/_footer.html.twig') }}
</body>
</html>
